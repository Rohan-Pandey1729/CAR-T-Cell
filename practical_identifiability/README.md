# Practical identifiability analysis with synthetic data and Monolix

TODO: write

## Pipeline design

(insert description)

(insert diagram)

## Usage notes

To be able to run scripts that run Monolix, create a .env file in
`src/main` and include the following:

```bash
MONOLIX_PATH="/path/to/monolix/binary"
```

(where you should replace `/path/to/monolix/binary` with the filepath
for your Monolix binary).
